# Never prompt before fetching the version a repo pins in `packageManager`.
export COREPACK_ENABLE_DOWNLOAD_PROMPT=0
# Never let `corepack use`/`install` rewrite `packageManager` in a repo.
export COREPACK_ENABLE_AUTO_PIN=0
